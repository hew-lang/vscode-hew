fn main() {
    let value = Option.Some(5);
    let result = if let .Some(x) = value { x } else { 0 };
    if let .Some(x) = Option.Some(result) {
        println(x);
    } else {
        println(0);
    }
}
