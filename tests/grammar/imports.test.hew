// SYNTAX TEST "source.hew"

// -- Dotted path import --

import std.time.datetime;
// <- keyword.declaration.hew
//        ^ punctuation.accessor.path.hew
//             ^ punctuation.accessor.path.hew

// -- Dotted grouped selection with alias --

import a.b.{X, Y as Z};
// <- keyword.declaration.hew
//      ^ punctuation.accessor.path.hew
//        ^ punctuation.accessor.selection.hew
//         ^ punctuation.section.selection.begin.hew
//          ^ entity.name.type.hew
//               ^^ keyword.declaration.hew
//                  ^ entity.name.type.hew
//                   ^ punctuation.section.selection.end.hew

// -- Selection scope is import-only; this invalid expression stays ordinary punctuation --

let invalid = value.{Name};
//                 ^ punctuation.separator.dot.hew

// -- Retired glob import is invalid (E_IMPORT_GLOB_REMOVED) --

import a.b.*;
// <- keyword.declaration.hew
//        ^^ invalid.removed.hew

// -- Retired :: path is invalid (E_PATH_LEGACY_SEPARATOR) --

import std::time;
//        ^^ invalid.removed.hew

// -- Contextual enum variant --

match state {
    .Ready => 1,
//  ^ punctuation.accessor.variant.hew
//   ^^^^^ entity.name.variant.hew
}

// -- A qualified variant uses an ordinary dotted path --

let ready = State.Ready;
//               ^ punctuation.accessor.path.hew

// -- String-path import (grammar.ebnf:63) --

import "path.hew";
// <- keyword.declaration.hew
//     ^ string.quoted.double.hew
